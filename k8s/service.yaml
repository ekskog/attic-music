apiVersion: v1
kind: Service
metadata:
  name: attic-music
  namespace: webapps
  annotations:
    metallb.universe.tf/address-pool: cluster-pool
spec:
  type: LoadBalancer
  selector:
    app: attic-music
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: attic
  namespace: webapps
  annotations:
    metallb.universe.tf/address-pool: cluster-pool
spec:
  type: LoadBalancer
  selector:
    app: attic
  ports:
    - name: http
      port: 80
      targetPort: 80
